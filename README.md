# Marketing Analytics MVP

MVP-система для сквозной аналитики рекламных размещений и оценки их эффективности.

Проект позволяет связать рекламную кампанию и конкретное размещение с пользовательскими действиями, лидом и последующей покупкой. На основе этих данных система рассчитывает количество лидов, покупателей, выручку и ROMI по каждому рекламному размещению.

---

(быстрая информация)
## (14). Запуск проекта

### Быстрый запуск

Для запуска проекта нужны **3 терминала**.
**1. Backend**

```bash
python -m venv .venv
```

Windows:

```bash
.venv\Scripts\activate
```

Установить зависимости:

```bash
pip install fastapi uvicorn openpyxl pandas
```

Запустить сервер:

```bash
uvicorn backend.main:app --reload
```

Backend будет доступен по адресу:
http://127.0.0.1:8000

---

**2. Генерация данных**

Откройте **второй терминал** в корне проекта и выполните:

```bash
python backend/seed.py
```

После выполнения должен появиться файл:

```text
data/marketing.db
```

> Перед запуском убедитесь, что исходный файл находится по пути `data/base.xlsx`.

---

**3. Frontend**

Откройте **третий терминал**:

```bash
cd frontend
npm install
npm run dev
```

После запуска откройте адрес, который покажет Vite в терминале, например:

```text
http://localhost:5173
```

### Порядок запуска

```text
Terminal 1 → Backend
Terminal 2 → Генерация данных
Terminal 3 → Frontend
                         ↓
                  Открыть браузер
```


## 1. Цель проекта

Проект реализует техническую основу будущей системы маркетинговой аналитики.

Основная задача — построить цепочку:

```text
Рекламная кампания
        ↓
Рекламное размещение
        ↓
Переход пользователя
        ↓
Взаимодействие с ботом
        ↓
Лид
        ↓
Покупка
        ↓
Атрибуция
        ↓
Выручка и ROMI
```

MVP позволяет ответить на ключевой вопрос:

> Какое рекламное размещение привело пользователя к покупке и какую выручку оно принесло?

---

# 2. Выполненные задачи

## Задание 3 — Техническая модель системы

Спроектирована структура хранения данных для рекламных кампаний, размещений, пользовательских действий и покупок.

Система разделяет:

* рекламные кампании;
* отдельные рекламные размещения;
* рекламные материалы;
* стоимость размещения;
* время публикации;
* пользовательские касания;
* лиды;
* покупки;
* связь между рекламным касанием и покупкой.

Это позволяет не смешивать данные о рекламе и данные о продажах и в дальнейшем расширять систему без изменения основной логики.

---

## Задание 4 — События и атрибуция

Для отслеживания пользовательского пути используется последовательность маркетинговых событий:

```text
click
   ↓
bot_start
   ↓
lead
   ↓
purchase
```

Каждое маркетинговое событие содержит информацию о пользователе, размещении и времени события.

После совершения покупки система определяет маркетинговое касание, которое необходимо использовать для атрибуции.

В MVP используется принцип **Last Touch Attribution**:

> покупка относится к последнему известному маркетинговому касанию пользователя перед покупкой.

При этом одна покупка может быть атрибутирована только одному касанию.

---

## Задание 5 — Рабочий MVP

На основе спроектированной модели реализован запускаемый MVP.

MVP:

1. загружает реальные обезличенные продажи из `base.xlsx`;
2. создаёт таблицу продаж;
3. создаёт тестовые рекламные кампании;
4. создаёт рекламные размещения;
5. генерирует тестовые пользовательские события;
6. формирует цепочки `click → bot_start → lead`;
7. связывает лидов с покупками;
8. выполняет атрибуцию;
9. рассчитывает выручку;
10. рассчитывает ROMI по размещениям.

Результат можно использовать для построения аналитического дашборда.

---

# 3. Архитектура проекта

Проект построен как небольшой data/analytics MVP на Python и SQLite.

```text
marketing-mvp/
│
├── backend/
│   └── seed.py
│
├── data/
│   ├── base.xlsx
│   └── marketing.db
│
└── README.md
```

### `backend/seed.py`

Основной скрипт проекта.

Отвечает за:

* загрузку исходных продаж;
* создание структуры БД;
* генерацию рекламных данных;
* генерацию пользовательских событий;
* атрибуцию покупок;
* заполнение базы данных.

### `data/base.xlsx`

Исходный обезличенный слой продаж.

Файл содержит реальные данные о покупках.

Используемые поля:

| Поле             | Назначение                            |
| ---------------- | ------------------------------------- |
| `номер студента` | обезличенный идентификатор покупателя |
| `сумма`          | сумма покупки                         |
| `курс`           | приобретённый курс                    |
| `время`          | время покупки                         |

В БД эти поля преобразуются в:

| Excel            | SQLite         |
| ---------------- | -------------- |
| `номер студента` | `student_id`   |
| `сумма`          | `amount`       |
| `курс`           | `course`       |
| `время`          | `purchased_at` |

### `data/marketing.db`

SQLite-база данных, создаваемая автоматически при запуске MVP.

---

# 4. Модель данных

## 4.1. `sales`

Таблица фактических покупок.

```text
sales
├── sale_id
├── student_id
├── amount
├── course
└── purchased_at
```

### Назначение

Хранит исходные данные о продажах, импортированные из `base.xlsx`.

`student_id` — обезличенный идентификатор покупателя.

`amount` — сумма покупки.

`sale_id` — уникальный идентификатор конкретной покупки.

---

# 5. Рекламные кампании

## `synthetic_campaigns`

Хранит информацию о рекламных кампаниях.

```text
synthetic_campaigns
├── campaign_id
├── name
├── channel
├── start_date
├── end_date
└── budget
```

Пример:

```text
CAMP_TG_ML
Промо ML курса
Telegram
2026-08-05 — 2026-09-15
100 000 ₽
```

### Зачем отдельная таблица кампаний?

Одна кампания может содержать несколько рекламных размещений.

Например:

```text
Промо ML курса
│
├── Data Science Today
└── AI & ML Community
```

Это позволяет анализировать эффективность как отдельной кампании, так и конкретного размещения.

---

# 6. Рекламные размещения

## `synthetic_placements`

Каждая запись представляет конкретное рекламное размещение.

```text
synthetic_placements
├── placement_id
├── campaign_id
├── channel
├── placement_name
├── creative_id
├── tracking_id
├── published_at
└── cost
```

### Основные поля

**`placement_id`**

Уникальный идентификатор размещения.

**`campaign_id`**

Связь с рекламной кампанией.

**`placement_name`**

Площадка или источник размещения.

Например:

```text
Data Science Today
AI & ML Community
Junior Analyst
```

**`creative_id`**

Идентификатор рекламного креатива.

Позволяет в будущем анализировать эффективность конкретных рекламных материалов.

**`tracking_id`**

Идентификатор рекламной ссылки или метки.

Именно его можно использовать в реальной системе для определения источника перехода.

**`published_at`**

Время публикации рекламы.

Это важно для проверки корректности пользовательского пути:

```text
публикация рекламы
        ↓
переход
        ↓
лид
        ↓
покупка
```

**`cost`**

Фактическая стоимость размещения.

---

# 7. Пользовательские маркетинговые события

## `marketing_touches`

Таблица хранит все маркетинговые касания пользователя.

```text
marketing_touches
├── touch_id
├── student_id
├── placement_id
├── event_type
└── touched_at
```

### Типы событий

В MVP используются:

```text
click
bot_start
lead
```

Например, путь пользователя может выглядеть так:

```text
student_123

10:00  click
10:01  bot_start
10:05  lead
11:30  purchase
```

Все события связаны с одним `student_id`.

Благодаря этому можно восстановить путь пользователя от рекламы до покупки.

---

# 8. Атрибуция покупок

## `attribution`

Таблица связывает конкретную покупку с маркетинговым касанием.

```text
attribution
├── attribution_id
├── sale_id
├── touch_id
├── attribution_model
└── attributed_amount
```

Основная связь:

```text
marketing_touches
        │
        │ touch_id
        ▼
   attribution
        │
        │ sale_id
        ▼
      sales
```

Таким образом система может ответить:

> Какое рекламное касание привело к этой покупке?

---

# 9. Модель атрибуции

В MVP используется **Last Touch Attribution**.

Логика:

1. пользователь взаимодействует с рекламой;
2. система записывает маркетинговое событие;
3. пользователь становится лидом;
4. пользователь совершает покупку;
5. система ищет последнее маркетинговое касание перед покупкой;
6. выручка покупки относится к этому касанию и его рекламному размещению.

Пример:

```text
Data Science Today
        ↓
      click
        ↓
    bot_start
        ↓
       lead
        ↓
     purchase
      7 500 ₽
```

Результат:

```text
Data Science Today
Revenue = 7 500 ₽
```

Одна покупка не должна приносить выручку нескольким размещениям.

---

# 10. Генерация тестовых данных

Поскольку исходный файл содержит только информацию о продажах, в MVP дополнительно генерируются синтетические маркетинговые данные.

Это необходимо, потому что в `base.xlsx` отсутствует информация о:

* рекламных кампаниях;
* размещениях;
* переходах;
* лидах;
* рекламных креативах;
* tracking ID;
* стоимости рекламы.

Поэтому эти данные создаются программно.

При этом покупки остаются основаны на исходном `base.xlsx`.

Получается комбинация:

```text
Реальные обезличенные продажи
             +
Синтетические маркетинговые события
             ↓
       Аналитический MVP
```

---

# 11. Расчёт показателей

## Лиды

Количество уникальных пользователей, дошедших до события `lead`.

```text
Leads = COUNT(DISTINCT student_id)
```

---

## Покупатели

Количество уникальных пользователей, совершивших атрибутированную покупку.

```text
Buyers = COUNT(DISTINCT student_id)
```

---

## Выручка

Сумма покупок, атрибутированных рекламному размещению.

```text
Revenue = SUM(attributed_amount)
```

---

## ROMI

Основная метрика эффективности рекламы:

```text
ROMI = (Revenue - Cost) / Cost × 100%
```

Например:

```text
Расходы = 15 000 ₽
Выручка = 709 867 ₽

ROMI =
(709 867 - 15 000) / 15 000 × 100%

= 4 632,4%
```

Высокий ROMI сам по себе не является ошибкой: он зависит от размера среднего чека, количества атрибутированных покупок и стоимости рекламного размещения.

---

# 12. Итоговая аналитика

После генерации данных можно получить таблицу вида:

| Кампания        | Размещение         |   Расход | Лиды | Покупатели |   Выручка |  ROMI |
| --------------- | ------------------ | -------: | ---: | ---------: | --------: | ----: |
| Промо ML курса  | Data Science Today | 15 000 ₽ |  122 |         95 | 709 867 ₽ | 4632% |
| Аналитика старт | Аналитика для всех | 12 000 ₽ |  127 |         94 | 643 572 ₽ | 5263% |
| Аналитика старт | Junior Analyst     | 18 000 ₽ |   86 |         52 | 438 583 ₽ | 2337% |

Таким образом, MVP позволяет сравнивать рекламные размещения не только по количеству переходов или лидов, но и по фактической выручке.

---

# 13. Почему используется SQLite

Для MVP выбран SQLite.

Преимущества:

* не требует отдельного сервера БД;
* запускается локально;
* не требует настройки PostgreSQL/MySQL;
* поддерживается стандартными средствами Python;
* удобно использовать для демонстрационного проекта;
* база хранится одним файлом.

Для production-системы SQLite можно заменить на PostgreSQL без изменения общей логики модели данных.

---

# 14. Запуск проекта

## 14.1. Требования

Для запуска проекта необходимо установить:

* Python 3.10 или выше;
* `pip`;
* Git.

Проверить установку:

```bash
python --version
pip --version
git --version
```

Для Windows также рекомендуется использовать PowerShell или терминал VS Code.

---

## 14.2. Клонирование репозитория

Клонировать проект:

```bash
git clone <URL_РЕПОЗИТОРИЯ>
```

Перейти в директорию проекта:

```bash
cd marketing-mvp
```

---

## 14.3. Создание виртуального окружения

Создаём отдельное виртуальное окружение Python:

### Windows

```powershell
python -m venv .venv
```

Активируем его:

```powershell
.venv\Scripts\activate
```

### Linux / macOS

```bash
python3 -m venv .venv
source .venv/bin/activate
```

---

## 14.4. Установка зависимостей

После активации виртуального окружения установить необходимые библиотеки:

```bash
pip install openpyxl
```

Запуск Sql в папке бэкэнд

```bash
uvicorn main:app --reload
```


---

## 14.5. Подготовка исходных данных

Для работы MVP необходим исходный файл:

```text
data/base.xlsx
```

Файл содержит обезличенные данные о покупках.

Ожидаемые колонки:

```text
номер студента
сумма
курс
время
```

Пример:

| номер студента | сумма | курс      | время               |
| -------------- | ----: | --------- | ------------------- |
| student_001    |  7500 | Python    | 2026-08-15 12:30:00 |
| student_002    |  5000 | Аналитика | 2026-08-16 14:20:00 |

Скрипт автоматически преобразует эти поля в структуру таблицы `sales`:

```text
номер студента → student_id
сумма          → amount
курс           → course
время          → purchased_at
```

**Важно:** файл должен находиться именно по пути:

```text
data/base.xlsx
```

---

## 14.6. Создание базы данных

Отдельно создавать SQLite-базу вручную не требуется.

При запуске `seed.py` база создаётся автоматически.

Файл базы:

```text
data/marketing.db
```

---

## 14.7. Запуск генерации данных

Запускать скрипт необходимо из корневой директории проекта:

```bash
python backend/seed.py
```

Скрипт выполняет следующие действия:

1. открывает `data/base.xlsx`;
2. читает исходные продажи;
3. создаёт таблицу `sales`;
4. импортирует продажи в SQLite;
5. создаёт рекламные кампании;
6. создаёт рекламные размещения;
7. создаёт тестовые маркетинговые события;
8. формирует пользовательские воронки;
9. выполняет атрибуцию покупок;
10. рассчитывает контрольные показатели;
11. сохраняет результат в `data/marketing.db`.

---

## 14.8. Что создаётся в базе

После выполнения скрипта в базе будут созданы основные таблицы:

```text
sales
synthetic_campaigns
synthetic_placements
marketing_touches
attribution
```

### `sales`

Фактические покупки из `base.xlsx`.

### `synthetic_campaigns`

Тестовые рекламные кампании.

### `synthetic_placements`

Тестовые рекламные размещения с информацией о:

* кампании;
* площадке;
* креативе;
* tracking ID;
* времени публикации;
* стоимости размещения.

### `marketing_touches`

Пользовательские маркетинговые события:

```text
click
bot_start
lead
```

### `attribution`

Связь маркетингового касания с конкретной покупкой.

---

## 14.9. Проверка успешного запуска

После запуска в консоли должна появиться статистика обработки.

В частности, можно проверить:

* количество импортированных продаж;
* количество уникальных студентов;
* количество созданных маркетинговых событий;
* количество лидов;
* количество атрибутированных покупок;
* сумму атрибутированной выручки.

Также должен появиться файл:

```text
data/marketing.db
```

Если файл существует и скрипт завершился без traceback/ошибки — генерация базы выполнена успешно.

---

## 14.10. Повторный запуск

Скрипт можно запускать повторно.

При повторном запуске исходные продажи снова импортируются из:

```text
data/base.xlsx
```

а синтетические таблицы пересоздаются/заполняются заново.

Это позволяет получить новый набор тестовых маркетинговых событий.

Поскольку генерация использует случайные значения, результаты разных запусков могут отличаться.

---

## 14.11. Просмотр SQLite-базы

Получившийся файл:

```text
data/marketing.db
```

можно открыть через любой SQLite-клиент.

Например, можно использовать:

* SQLiteStudio;
* DBeaver;
* DB Browser for SQLite;
* расширение SQLite для VS Code.

После открытия базы должны быть доступны таблицы:

```text
sales
synthetic_campaigns
synthetic_placements
marketing_touches
attribution
```

---

## 14.12. Проверка данных через SQL

Количество продаж:

```sql
SELECT COUNT(*)
FROM sales;
```

Количество уникальных покупателей:

```sql
SELECT COUNT(DISTINCT student_id)
FROM sales;
```

Количество рекламных размещений:

```sql
SELECT COUNT(*)
FROM synthetic_placements;
```

Количество маркетинговых событий:

```sql
SELECT COUNT(*)
FROM marketing_touches;
```

Количество атрибуций:

```sql
SELECT COUNT(*)
FROM attribution;
```

---

## 14.13. Получение итоговой аналитики

Основной результат работы MVP — аналитика по рекламным размещениям.

Для каждого размещения рассчитываются:

```text
Расходы
Лиды
Покупатели
Выручка
ROMI
```

ROMI рассчитывается по формуле:

```text
ROMI = (Revenue - Cost) / Cost × 100%
```

Например:

```text
Расходы: 15 000 ₽
Выручка: 709 867 ₽

ROMI = (709 867 - 15 000) / 15 000 × 100%
     = 4 632,4%
```

---

## 14.14. Полный сценарий запуска

Для нового пользователя весь процесс сводится к следующим командам:

```powershell
git clone <URL_РЕПОЗИТОРИЯ>
cd marketing-mvp

python -m venv .venv
.venv\Scripts\activate

pip install -r requirements.txt

python backend/seed.py
```

Если `requirements.txt` отсутствует:

```powershell
pip install openpyxl
python backend/seed.py
```

После выполнения:

```text
data/marketing.db
```

содержит подготовленные данные для дальнейшего анализа и построения дашборда.

---

## 14.15. Типичные ошибки

### `ModuleNotFoundError: No module named 'openpyxl'`

Установить зависимость:

```bash
pip install openpyxl
```

---

### `FileNotFoundError: base.xlsx`

Проверить, что файл находится здесь:

```text
marketing-mvp/
└── data/
    └── base.xlsx
```

---

### `python is not recognized`

Проверить установку Python:

```bash
python --version
```

Если команда не работает, установить Python и добавить его в `PATH`.

---

### Ошибка при повторном запуске

Удалить созданную базу:

```text
data/marketing.db
```

и снова выполнить:

```bash
python backend/seed.py
```

---

## 14.16. Итог

После выполнения инструкции система полностью готова к работе:

```text
base.xlsx
    ↓
Импорт продаж
    ↓
SQLite
    ↓
Рекламные кампании
    ↓
Рекламные размещения
    ↓
Маркетинговые события
    ↓
Лиды
    ↓
Покупки
    ↓
Атрибуция
    ↓
Выручка
    ↓
ROMI
```

Таким образом, для запуска MVP пользователю достаточно установить Python, установить зависимости, положить `base.xlsx` в `data/` и выполнить:

```bash
python backend/seed.py
```

# 15. Проверка базы данных

Для просмотра базы можно использовать любой SQLite-клиент.

Например, после запуска в базе будут доступны таблицы:

```text
sales
synthetic_campaigns
synthetic_placements
marketing_touches
attribution
```

Проверить количество продаж:

```sql
SELECT COUNT(*)
FROM sales;
```

Проверить количество рекламных размещений:

```sql
SELECT COUNT(*)
FROM synthetic_placements;
```

Проверить маркетинговые события:

```sql
SELECT
    event_type,
    COUNT(*)
FROM marketing_touches
GROUP BY event_type;
```

Проверить атрибуцию:

```sql
SELECT COUNT(*)
FROM attribution;
```

---

# 16. Проверки корректности

При генерации данных выполняются проверки:

* количество атрибуций соответствует количеству уникальных атрибутированных покупок;
* один `sale_id` не должен быть атрибутирован несколько раз;
* покупатель не должен появляться в атрибуции без соответствующей покупки;
* маркетинговое событие должно происходить после публикации размещения;
* покупка должна происходить после маркетингового касания;
* количество покупателей не должно превышать количество лидов для соответствующей воронки.

Это позволяет использовать MVP не только как демонстрацию, но и как основу для дальнейшего развития системы.

---

# 17. Возможное развитие системы

Текущий MVP является минимальной рабочей версией. В production-системе его можно расширить.

### Реальные рекламные данные

Подключить API рекламных площадок:

```text
Telegram Ads
VK Ads
Яндекс Директ
Google Ads
```

---

### Реальный tracking

Использовать UTM-метки:

```text
utm_source
utm_medium
utm_campaign
utm_content
utm_term
```

и собственный `tracking_id`.

---

### Реальные пользовательские события

Вместо синтетической генерации получать события из:

```text
Web
Telegram Bot
CRM
Backend
Payment System
```

---

### Другие модели атрибуции

Можно добавить:

* First Touch;
* Last Touch;
* Linear Attribution;
* Time Decay;
* Position Based;
* Data Driven Attribution.

---

### Production БД

SQLite можно заменить на:

```text
PostgreSQL
```

для одновременной работы большого количества пользователей и сервисов.

---

### Аналитический слой

Поверх базы можно построить:

```text
Backend API
      ↓
Dashboard
      ↓
Campaign Analytics
```

с графиками:

* расходы;
* лиды;
* конверсия;
* покупатели;
* выручка;
* ROMI;
* стоимость лида;
* стоимость привлечения покупателя.

---

# 18. Результат

В рамках проекта реализована рабочая техническая основа системы маркетинговой атрибуции.

Система позволяет пройти полный путь:

```text
Campaign
    ↓
Placement
    ↓
Creative
    ↓
Tracking ID
    ↓
Click
    ↓
Bot Start
    ↓
Lead
    ↓
Purchase
    ↓
Attribution
    ↓
Revenue
    ↓
ROMI
```

Таким образом, задания **3, 4 и 5**, а также требование по созданию рабочего **MVP**, закрыты в единой архитектуре, которую можно запускать локально и расширять до полноценной системы сквозной маркетинговой аналитики.



### Статус данных

| Метрика           | Источник     |
| ----------------- | ------------ |
| Покупки           | 🟢 Real      |
| Выручка           | 🟢 Real      |
| Покупатели        | 🟢 Real      |
| Курсы             | 🟢 Real      |
| Повторные покупки | 🟢 Real      |
| Рекламные касания | 🟡 Synthetic |
| Каналы            | 🟡 Synthetic |
| Placement         | 🟡 Synthetic |
| Cost              | 🟡 Synthetic |
| ROMI              | 🟡 Demo      |


